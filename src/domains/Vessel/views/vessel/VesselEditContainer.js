import React from "react";
import VesselEdit from "../../components/vessel/VesselEdit";

const VesselEditContainer = (props) => {
    return (
        <>
            <VesselEdit props={props} />
        </>
    );
};

export default VesselEditContainer;
